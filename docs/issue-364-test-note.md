# Prueba manual sugerida

Abrir una orden con múltiples destinos/pallets desde la webapp y verificar que cada renglón de despacho muestre el contexto correcto y que lote/fecha aparezcan una sola vez por `LoadOrderProduct`.
